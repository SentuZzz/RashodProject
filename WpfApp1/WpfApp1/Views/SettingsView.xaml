<UserControl x:Class="WpfApp1.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левое навигационное меню -->
        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,20,0" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Справочники" FontSize="20" FontWeight="Bold" Foreground="#344E41" Margin="10,10,0,25"/>

                <ListBox ItemsSource="{Binding MenuItems}" SelectedItem="{Binding SelectedMenu}" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="15,12"/>
                            <Setter Property="Margin" Value="0,0,0,8"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E2E8F0"/>
                                    <Setter Property="Foreground" Value="#0F172A"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding SelectedMenu}" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                    <ItemsControl ItemsSource="{Binding CurrentItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="8" Padding="20,12" Margin="0,0,0,10">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#334155" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            <!-- Этот блок виден только для нарядов -->
                                            <Border Background="#E0E7FF" CornerRadius="6" Padding="8,2" Margin="15,0,0,0" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDuty}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <!-- ВЫВОДИМ ЛОКАЦИЮ И ПРИОРИТЕТ -->
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Location, StringFormat='Место: {0} | '}" FontSize="12" Foreground="#4338CA" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding PriorityText, StringFormat='Категория: {0}'}" FontSize="12" Foreground="#4338CA" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>

                                        <!-- Кнопки действий -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Button Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                     CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Редактировать" Margin="0,0,10,0">
                                                <fa:IconImage Icon="PenToSquare" Foreground="#3B82F6" Width="16" Height="16"/>
                                            </Button>
                                            <Button Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                     CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Удалить">
                                                <fa:IconImage Icon="TrashAlt" Foreground="#EF4444" Width="16" Height="16"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Форма добавления/редактирования (ОБЫЧНЫЕ СПРАВОЧНИКИ) -->
                <Border Grid.Row="2" Visibility="{Binding SimpleFormVisibility}" Background="#F1F5F9" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" Height="40" Padding="15,0" VerticalContentAlignment="Center" FontSize="15" Margin="0,0,15,0">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>

                        <Button Grid.Column="1" Content="{Binding SubmitButtonText}" Command="{Binding SaveItemCommand}" Background="#344E41" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Padding="25,0" Height="40" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Grid.Column="2" Content="Отмена" Command="{Binding CancelEditCommand}" Visibility="{Binding CancelButtonVisibility}" Background="#EF4444" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Padding="15,0" Height="40" Margin="10,0,0,0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <!-- Форма добавления/редактирования (ДЛЯ НАРЯДОВ) -->
                <Border Grid.Row="2" Visibility="{Binding DutyFormVisibility}" Background="#F1F5F9" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <!-- Первая строка полей -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Название наряда:" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10,0" VerticalContentAlignment="Center" FontSize="14">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Место (Группа):" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding NewDutyLocation, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10,0" VerticalContentAlignment="Center" FontSize="14">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>
                            </Grid>
                            <!-- Вторая строка полей -->
                            <TextBlock Text="Кто заступает (Категория):" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding PriorityOptions}" SelectedItem="{Binding SelectedPriority}" DisplayMemberPath="Title" Height="35"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="{Binding SubmitButtonText}" Command="{Binding SaveItemCommand}" Background="#344E41" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Padding="25,0" Height="40" VerticalAlignment="Bottom" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Grid.Column="2" Content="Отмена" Command="{Binding CancelEditCommand}" Visibility="{Binding CancelButtonVisibility}" Background="#EF4444" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Padding="15,0" Height="40" Margin="10,0,0,0" VerticalAlignment="Bottom" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

            </Grid>
        </Border>
    </Grid>
</UserControl>