<UserControl x:Class="WpfApp1.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Шапка с часами -->
            <RowDefinition Height="Auto"/>
            <!-- Карточки статистики -->
            <RowDefinition Height="*"/>
            <!-- Место под наряды на завтра -->
        </Grid.RowDefinitions>

        <!-- 1. ШАПКА И ЖИВЫЕ ЧАСЫ -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="ГЛАВНАЯ ПАНЕЛЬ" FontSize="26" FontWeight="Bold" Foreground="{StaticResource secondaryBackBrush}" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="{Binding CurrentDate}" FontSize="16" Foreground="#7F8C8D" Margin="0,0,15,3" VerticalAlignment="Bottom"/>
                <TextBlock Text="{Binding CurrentTime}" FontSize="28" FontWeight="Bold" Foreground="#344E41" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Grid>

        <!-- 2. КАРТОЧКИ СТАТИСТИКИ (БЛОК 1) -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Карточка 1: По списку -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="50" Height="50" CornerRadius="25" Background="#F1F5F9">
                        <fa:IconImage Icon="Users" Foreground="#475569" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="По списку" Foreground="#94A3B8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalCount}" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Карточка 2: В строю -->
            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="50" Height="50" CornerRadius="25" Background="#DCFCE7">
                        <fa:IconImage Icon="UserCheck" Foreground="#16A34A" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="В строю (налицо)" Foreground="#94A3B8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding InFormationCount}" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Карточка 3: Отсутствуют -->
            <Border Grid.Column="4" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="50" Height="50" CornerRadius="25" Background="#FEE2E2">
                        <fa:IconImage Icon="UserMinus" Foreground="#DC2626" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Отсутствуют" Foreground="#94A3B8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding AbsentCount}" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Карточка 4: В наряде -->
            <Border Grid.Column="6" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="50" Height="50" CornerRadius="25" Background="#E0F2FE">
                        <fa:IconImage Icon="ShieldHalved" Foreground="#0284C7" Width="24" Height="24"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="В наряде" Foreground="#94A3B8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding OnDutyCount}" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- ЛЕВАЯ ЧАСТЬ: НАРЯДЫ НА ЗАВТРА (Тревожная панель) -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <fa:IconImage Icon="Bell" Foreground="#F59E0B" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="НАРЯДЫ НА ЗАВТРА (КОНТРОЛЬ)" FontSize="16" FontWeight="Bold" Foreground="#344E41" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding TomorrowDuties}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Иконка статуса -->
                                            <fa:IconImage Grid.Column="0" Icon="{Binding IconName}" Foreground="{Binding StatusColor}" Width="26" Height="26" VerticalAlignment="Center"/>

                                            <!-- Название и прогресс -->
                                            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DutyName}" FontSize="15" FontWeight="SemiBold" Foreground="#1E293B"/>
                                                <TextBlock Foreground="#64748B" FontSize="13" Margin="0,2,0,0">
                                                    <Run Text="Назначено: "/>
                                                    <Run Text="{Binding Assigned}" FontWeight="Bold" Foreground="#0F172A"/>
                                                    <Run Text=" из "/>
                                                    <Run Text="{Binding Capacity}"/>
                                                </TextBlock>
                                            </StackPanel>

                                            <!-- Текст статуса -->
                                            <Border Grid.Column="2" Background="White" CornerRadius="6" Padding="12,6" VerticalAlignment="Center">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="5" Color="Black" Opacity="0.05" ShadowDepth="1"/>
                                                </Border.Effect>
                                                <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontWeight="Bold" FontSize="12"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- ПРАВАЯ ЧАСТЬ: БЛИЖАЙШИЕ УВЕДОМЛЕНИЯ (Заглушка для следующего шага) -->
            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.05" ShadowDepth="3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <fa:IconImage Icon="CalendarAlt" Foreground="#3B82F6" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="УВЕДОМЛЕНИЯ" FontSize="16" FontWeight="Bold" Foreground="#344E41" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding UpcomingEvents}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <!-- 1. Увеличили ширину до 85, чтобы влезло "Послезавтра" -->
                                                <ColumnDefinition Width="85"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Дата -->
                                            <!-- 2. Убрали HorizontalAlignment="Left", чтобы плашки были строго одинаковой ширины -->
                                            <Border Grid.Column="0" Background="#F1F5F9" CornerRadius="4" Padding="2,5" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DateString}" FontSize="11" FontWeight="Bold" Foreground="#64748B" HorizontalAlignment="Center"/>
                                            </Border>

                                            <!-- Текст уведомления -->
                                            <!-- Немного увеличили левый отступ (Margin) для красоты -->
                                            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <fa:IconImage Icon="{Binding IconName, Mode=OneWay}" Foreground="{Binding IconColor, Mode=OneWay}" Width="14" Height="14" Margin="0,0,6,0"/>
                                                    <TextBlock Text="{Binding SoldierName}" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                                                </StackPanel>
                                                <TextBlock Margin="20,2,0,0" FontSize="12" Foreground="#64748B">
                                                    <Run Text="{Binding ActionText, Mode=OneWay}"/>
                                                    <Run Text="{Binding StatusName}" Foreground="{Binding IconColor, Mode=OneWay}" FontWeight="SemiBold"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- Что показать, если список пуст -->
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UpcomingEvents.Count}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <StackPanel HorizontalAlignment="Center" Margin="0,30,0,0">
                                                            <fa:IconImage Icon="CheckDouble" Foreground="#CBD5E1" Width="32" Height="32" HorizontalAlignment="Center"/>
                                                            <TextBlock Text="На ближайшие 3 дня изменений нет" Foreground="#94A3B8" FontStyle="Italic" Margin="0,10,0,0"/>
                                                        </StackPanel>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>