<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp">

    <!-- Стиль для RadioButton в меню -->
    <Style x:Key="menuButton" TargetType="RadioButton">
        <Setter Property="Height" Value="50"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource plainTextColor1}"/>
        <!-- Обычный цвет текста/иконки -->
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <!-- Убрали лишнюю границу сбоку, т.к. в дизайне её нет -->
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Стиль при наведении мыши (когда кнопка НЕ активна) -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource panelOverColor}"/>
                <Setter Property="Foreground" Value="{StaticResource titleColor1}"/>
            </Trigger>

            <!-- Стиль для АКТИВНОЙ кнопки (IsChecked = True) -->
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{StaticResource panelActiveColor}"/>
                <!-- Вот ключевое исправление: задаем цвет напрямую, а не через Tag -->
                <Setter Property="Foreground" Value="{StaticResource panelColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Иконка для кнопки меню -->
    <Style x:Key="menuButtonIcon" TargetType="fa:IconImage">
        <!-- Теперь иконка всегда берет цвет у родительской кнопки. Это правильно и гибко. -->
        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/>
        <Setter Property="Width" Value="22"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Margin" Value="25,0,20,0"/>
        <!-- Уменьшил отступ слева для лучшего вида -->
    </Style>

    <!-- Текст для кнопки меню -->
    <Style x:Key="menuButtonText" TargetType="TextBlock">
        <!-- Текст тоже всегда берет цвет у родительской кнопки. -->
        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/>
        <Setter Property="FontFamily" Value="Montserrat"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
</ResourceDictionary>
